{
  "name": "overengineered-design-system",
  "version": "0.0.0",
  "private": true,
  "repository": "ninetails/overengineered-design-system",
  "license": "MIT",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "devDependencies": {
    "@commitlint/cli": "^8.2.0",
    "@commitlint/config-conventional": "^8.2.0",
    "husky": "^4.0.0-beta.5",
    "lerna": "^3.19.0",
    "rimraf": "^3.0.0"
  },
  "scripts": {
    "clean": "lerna run clean && lerna clean -y && rimraf node_modules || :",
    "reset": "$npm_execpath run clean && $npm_execpath install"
  }
}
